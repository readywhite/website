{
  "name": "readywhite-ghl",
  "version": "1.0.0",
  "description": "Ready White landing page and GoHighLevel lead integration for Railway deployment.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check api/ghl-lead.js",
    "ops:check": "bash scripts/ops-system-check.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
